---
import ObituaryForm from '~/components/obituary-form/ObituaryForm.astro';
import Layout from '~/layouts/layout.astro';
import { ui } from '~/i18n/ui.ts';

const lang = "en";
const labels = ui[lang];
---
<Layout title={labels.title}>
  <ObituaryForm labels={labels} lang={lang} />
</Layout>